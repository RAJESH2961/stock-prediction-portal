[tool.poetry]
name = "stock-prediction-portal"
version = "0.1.0"
description = "Simple Django stock predictor"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
Django = "^4.2"
gunicorn = "^21.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
